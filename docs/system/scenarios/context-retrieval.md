---
generated: true
generated_from: capabilities/context-retrieval.md
type: scenario-set
status: draft
source_of_truth: true
sensitivity: internal
owner: cod-doc core
---

# Test scenarios — context-retrieval

## Overview

<!-- generated by scenario_service — edit via `cod-doc scenario`, not this file -->

Test scenarios for the **context-retrieval** capability — the authoring half of
[RFC 24 §9]. Coverage verdicts are not recorded here: whether a test proves a
scenario is producer-derived evidence, kept separately.

Capability document: `docs/system/capabilities/context-retrieval.md`

| Kind | Count |
|---|---|
| `happy_path` | 1 |
| `error_path` | 1 |
| `boundary_value` | 1 |
| `invariant` | 1 |
| `integration` | 1 |
| **total** | **5** |

## SCN-036 — context_get L0 returns metadata without pulling L1 bodies

**Kind:** `happy_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/context-retrieval#0-as-implemented-2026-09-15`

### Preconditions

A document target exists (e.g. MASTER).

### Steps

1. Call MCP `context_get` with depth `L0`

### Expected result

The payload includes target metadata and stays within the token budget without L2 chains.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-037 — Invalid context depth is refused

**Kind:** `error_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/context-retrieval#0-as-implemented-2026-09-15`

### Preconditions

A valid target_id exists.

### Steps

1. Call `context_get` with depth `L9`

### Expected result

`ValueError` says expected one of L0, L1, L2, L3.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-038 — L3 without an embedder returns empty semantic hits

**Kind:** `boundary_value`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/context-retrieval#0-as-implemented-2026-09-15`

### Preconditions

Embeddings are not configured (no embedder / placeholder key).

### Steps

1. Call `context_get` with depth `L3`

### Expected result

`related.semantic` is `[]`; the call does not fail.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-039 — hints.open_questions is always an empty list

**Kind:** `invariant`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/context-retrieval#0-as-implemented-2026-09-15`

### Preconditions

Open-question documents may or may not exist.

### Steps

1. Call `context_get` on a document at L1

### Expected result

`hints.open_questions` is `[]` regardless of DocumentType.open-question rows.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-040 — MCP context_get is the live contract, not CLI `context get`

**Kind:** `integration`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/context-retrieval#0-as-implemented-2026-09-15`

### Preconditions

standard MCP and CLI are installed.

### Steps

1. List MCP tools
2. Run `cod-doc ctx --help`

### Expected result

MCP has `context_get`; CLI has `ctx docs|drift|search`, not `context get`.

### Notes

TSC-002. Claims match code as of 2026-09-15.
