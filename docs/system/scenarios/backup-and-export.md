---
generated: true
generated_from: capabilities/backup-and-export.md
type: scenario-set
status: draft
source_of_truth: true
sensitivity: internal
owner: cod-doc core
---

# Test scenarios — backup-and-export

## Overview

<!-- generated by scenario_service — edit via `cod-doc scenario`, not this file -->

Test scenarios for the **backup-and-export** capability — the authoring half of
[RFC 24 §9]. Coverage verdicts are not recorded here: whether a test proves a
scenario is producer-derived evidence, kept separately.

Capability document: `docs/system/capabilities/backup-and-export.md`

| Kind | Count |
|---|---|
| `happy_path` | 1 |
| `error_path` | 1 |
| `boundary_value` | 1 |
| `invariant` | 1 |
| `integration` | 1 |
| **total** | **5** |

## SCN-056 — Projection export is the implemented export path

**Kind:** `happy_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/backup-and-export#0-as-implemented-2026-09-15`

### Preconditions

A document/plan/scenario-set exists and is not hand-edited.

### Steps

1. Run `cod-doc doc export` (or plan/scenario export) for that key

### Expected result

Markdown is written from the DB; this is the live export, not `cod-doc backup`.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-057 — Export from a foreign checkout is refused

**Kind:** `error_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/backup-and-export#0-as-implemented-2026-09-15`

### Preconditions

The configured project `root_path` is not this checkout, or another clone holds the files.

### Steps

1. Call scenario/doc export with `own_checkout_only=True` pointing at a foreign tree

### Expected result

`ExportGuardError` is raised.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-058 — There is no backup or restore CLI command

**Kind:** `boundary_value`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/backup-and-export#0-as-implemented-2026-09-15`

### Preconditions

The installed Click app is the current `cod-doc`.

### Steps

1. Run `cod-doc --help` and look for `backup` / `restore`

### Expected result

Those commands are absent.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-059 — Restoring the project means restoring state.db, not markdown

**Kind:** `invariant`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/backup-and-export#0-as-implemented-2026-09-15`

### Preconditions

Documents exist in SQLite and as projections.

### Steps

1. Delete a projection file without touching the DB
2. Read the document via `doc_get`

### Expected result

The DB row is intact; the missing file is drift `missing`, not data loss.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-060 — Scenario export reuses projection ExportGuardError

**Kind:** `integration`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/backup-and-export#0-as-implemented-2026-09-15`

### Preconditions

A scenario group projection was edited by hand.

### Steps

1. Call `scenario_export` without force

### Expected result

The same `ExportGuardError` family as `doc export` is raised.

### Notes

TSC-002. Claims match code as of 2026-09-15.
