---
generated: true
generated_from: capabilities/agents-and-skills.md
type: scenario-set
status: draft
source_of_truth: true
sensitivity: internal
owner: cod-doc core
---

# Test scenarios — agents-and-skills

## Overview

<!-- generated by scenario_service — edit via `cod-doc scenario`, not this file -->

Test scenarios for the **agents-and-skills** capability — the authoring half of
[RFC 24 §9]. Coverage verdicts are not recorded here: whether a test proves a
scenario is producer-derived evidence, kept separately.

Capability document: `docs/system/capabilities/agents-and-skills.md`

| Kind | Count |
|---|---|
| `happy_path` | 1 |
| `error_path` | 1 |
| `boundary_value` | 1 |
| `invariant` | 1 |
| `integration` | 1 |
| **total** | **5** |

## SCN-051 — agent_pick returns a task card with matched skills

**Kind:** `happy_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/agents-and-skills#0-as-implemented-2026-09-15`

### Preconditions

At least one ready task exists and is not locked.

### Steps

1. Call `agent_pick` with an agent_id

### Expected result

The payload contains the checked-out task and skill bodies from `cod_doc/skills/`.

### Notes

TSC-002. Claims match code as of 2026-09-15. ADO-217: tools live on `--profile standard|full` only; the default agent (doc curator, RFC 25 / CUR-008) does not take tasks.

## SCN-052 — agent_pick on an empty ready-set returns no_ready_tasks

**Kind:** `error_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/agents-and-skills#0-as-implemented-2026-09-15`

### Preconditions

The project has no ready (unblocked todo) tasks, or all are locked.

### Steps

1. Call `agent_pick` with a fresh agent_id

### Expected result

The result is `{"task": null, "reason": "no_ready_tasks"}`.

### Notes

TSC-002. Claims match code as of 2026-09-15. ADO-217: tools live on `--profile standard|full` only; the default agent (doc curator, RFC 25 / CUR-008) does not take tasks.

## SCN-053 — The agent MCP profile exposes exactly six tools

**Kind:** `boundary_value`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/agents-and-skills#0-as-implemented-2026-09-15`

### Preconditions

The server is started with `--profile agent`.

### Steps

1. Call `list_tools`

### Expected result

The catalog is `agent_capabilities`, `curator_next`, `ctx_search`, `ctx_drift`, `context_get`, `agent_report` (curator set, CUR-008; canon — tests/test_server_profiles.py).

### Notes

TSC-002. ADO-217: rewritten to the post-CUR-008 (2026-09-19) curator set; `agent_pick`/`agent_get`/`agent_complete`/`agent_release` moved to `standard`/`full`.

## SCN-054 — Agent behaviour is extended by SKILL.md, not the system prompt

**Kind:** `invariant`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/agents-and-skills#0-as-implemented-2026-09-15`

### Preconditions

A skill file exists under `cod_doc/skills/<name>/SKILL.md`.

### Steps

1. Call `skill_list` / `skill_get` for that name

### Expected result

The matcher returns that skill; there is no second copy inside a baked system prompt.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-055 — agent_complete closes the task through the same complete path

**Kind:** `integration`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/agents-and-skills#0-as-implemented-2026-09-15`

### Preconditions

An agent holds a checkout on an open task.

### Steps

1. Call `agent_complete` with a commit sha

### Expected result

The task is `done` via `task_service.complete` (revision + activity event).

### Notes

TSC-002. Claims match code as of 2026-09-15. ADO-217: tools live on `--profile standard|full` only; the default agent (doc curator, RFC 25 / CUR-008) does not take tasks.
