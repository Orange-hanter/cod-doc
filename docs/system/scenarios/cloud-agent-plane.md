---
generated: true
generated_from: capabilities/cloud-agent-plane.md
type: scenario-set
status: draft
source_of_truth: true
sensitivity: internal
owner: cod-doc core
---

# Test scenarios — cloud-agent-plane

## Overview

<!-- generated by scenario_service — edit via `cod-doc scenario`, not this file -->

Test scenarios for the **cloud-agent-plane** capability — the authoring half of
[RFC 24 §9]. Coverage verdicts are not recorded here: whether a test proves a
scenario is producer-derived evidence, kept separately.

Capability document: `docs/system/capabilities/cloud-agent-plane.md`

| Kind | Count |
|---|---|
| `happy_path` | 1 |
| `error_path` | 1 |
| `boundary_value` | 1 |
| `invariant` | 1 |
| `integration` | 1 |
| **total** | **5** |

## SCN-061 — streamable-http MCP binds 127.0.0.1 by default

**Kind:** `happy_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/cloud-agent-plane#0-as-implemented-2026-09-15`

### Preconditions

The MCP server is started with `--transport streamable-http` and default host.

### Steps

1. Inspect the listen address

### Expected result

The bind host is `127.0.0.1`.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-062 — POST /settings from a non-loopback client is 403

**Kind:** `error_path`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/cloud-agent-plane#0-as-implemented-2026-09-15`

### Preconditions

The web/API server is running.

### Steps

1. POST `/settings` with a non-loopback client address

### Expected result

The response is HTTP 403 `loopback-only` (SYM-003).

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-063 — streamable-http has no Bearer or TLS requirement

**Kind:** `boundary_value`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/cloud-agent-plane#0-as-implemented-2026-09-15`

### Preconditions

MCP is running with streamable-http on localhost.

### Steps

1. Connect without an Authorization header

### Expected result

The connection is accepted; there is no Bearer/TLS gate in this code path.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-064 — SoT remains local SQLite, not a cloud Postgres profile

**Kind:** `invariant`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/cloud-agent-plane#0-as-implemented-2026-09-15`

### Preconditions

A default project is opened.

### Steps

1. Resolve the DB URL for the project

### Expected result

The URL is embedded `<root>/.cod-doc/state.db` (or hub sqlite), not a cloud Postgres DSN required by this capability.

### Notes

TSC-002. Claims match code as of 2026-09-15.

## SCN-065 — The agent-profile tools work over local MCP

**Kind:** `integration`
**Status:** `draft`
**Anchor:** `docs/system/capabilities/cloud-agent-plane#0-as-implemented-2026-09-15`

### Preconditions

MCP is reachable via stdio or localhost streamable-http.

### Steps

1. Call `agent_capabilities` then `curator_next`

### Expected result

The agent profile tools (curator set) function without a remote cloud plane.

### Notes

TSC-002. Claims match code as of 2026-09-15. ADO-217: tools live on `--profile standard|full` only; the default agent (doc curator, RFC 25 / CUR-008) does not take tasks. Steps rewritten to the curator entry point: `agent_capabilities` then `curator_next`.
